/**
 * This package contains the implementation of the One vehicle Pickup and Delivery Problem with Time Window (PDPTW)
 * it has two twists:
 *-  it only supports one vehicle because DDO-like solvers have this limitation
 *-   it can incorporate unrelated nodes that are not within a pickup/delivery pair of node.
 * The package features a random generator
 */
package org.ddolib.examples.pdptw;
