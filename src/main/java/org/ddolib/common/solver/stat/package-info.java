/**
 * Provides classes for tracking and representing the statistics and status of a search process in a solver.
 * <p>
 * This package includes the base class {@link org.ddolib.common.solver.stat.SearchStatistics}
 * which tracks common metrics like runtime, iterations, and incumbent values, as well as
 * specialized implementations for specific solver variants like DDO and A*.
 * </p>
 */
package org.ddolib.common.solver.stat;